apiVersion: v1
kind: PersistentVolume
metadata:
  name: grafana-storage
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /data/grafana
  mountOptions:
    - uid=472 # Grafana process UID
    - gid=472 # Grafana process GID
