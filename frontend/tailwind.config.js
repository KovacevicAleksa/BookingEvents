/** @type {import('tailwindcss').Config} */
module.exports = {
  // Specify the paths to all of your template files
  content: ["./src/**/*.{js,jsx,ts,tsx}"], // Include all JS, JSX, TS, and TSX files within the 'src' directory

  theme: {
    extend: {}, // Extend the default Tailwind CSS theme if needed
  },

  plugins: [], // List of plugins to be used with Tailwind CSS
};
